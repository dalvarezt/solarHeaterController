'use strict';
const Logger = require('./controller/controller.js').Logger;
const Controller = require('./controller/controller.js').Controller;

var logger = new Logger();
var controller = new Controller();
